===
quote symbol
===

'x

---

(source
  (quote
    (symbol)))

===
quote list
===

'(1 2 3)

---

(source
  (quote
    (list
      (number)
      (number)
      (number))))

===
quasiquote with unquote and splice
===

`(a ~b ~@c)

---

(source
  (quasiquote
    (list
      (symbol)
      (unquote
        (symbol))
      (unquote_splicing
        (symbol)))))

===
nested quasiquote with splice
===

`(~@(list 1 2) 3)

---

(source
  (quasiquote
    (list
      (unquote_splicing
        (list
          (symbol)
          (number)
          (number)))
      (number))))

===
deref
===

@x

---

(source
  (deref
    (symbol)))

===
nested deref
===

@@x

---

(source
  (deref
    (deref
      (symbol))))

===
deref list
===

@(atom 42)

---

(source
  (deref
    (list
      (symbol)
      (number))))

===
discard form
===

[1 #_2 3]

---

(source
  (vector
    (number)
    (discard
      (number))
    (number)))

===
discard list
===

(+ 1 #_(* 2 3) 4)

---

(source
  (list
    (symbol)
    (number)
    (discard
      (list
        (symbol)
        (number)
        (number)))
    (number)))

===
shorthand fn
===

#(+ % 1)

---

(source
  (shorthand_fn
    (symbol)
    (symbol)
    (number)))

===
shorthand fn with map
===

(map #(* %1 %2) pairs)

---

(source
  (list
    (symbol)
    (shorthand_fn
      (symbol)
      (symbol)
      (symbol))
    (symbol)))

===
metadata with map
===

^{:private true} (defn helper [x] x)

---

(source
  (metadata
    (map
      (keyword)
      (boolean))
    (list
      (symbol)
      (symbol)
      (vector
        (symbol))
      (symbol))))

===
metadata keyword shorthand
===

^:private [1 2 3]

---

(source
  (metadata
    (keyword)
    (vector
      (number)
      (number)
      (number))))
